<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= page_title %></title>
  </head>
  <body>

    <h1>Data from the CSV</h1>

    <h3>We found <%= people.length %> people with an average age of <%= average_age %></h3>

    <ul>
      <% people.each do |person| %>
        <li><%= person["first_name"] %></li>
      <% end %>
    </ul>

  </body>
</html>
